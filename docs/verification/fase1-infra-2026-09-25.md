# Evidencia de conectividad — Fase 1

Fecha de ejecución: 2026-09-25

## Tailscale

Desde el entorno local se ejecutó:

```text
Test-NetConnection -ComputerName 100.89.59.70 -Port 5433 -InformationLevel Quiet
True

Test-NetConnection -ComputerName 100.89.59.70 -Port 6380 -InformationLevel Quiet
True
```

Esto confirma conectividad operativa a PostgreSQL y Redis por la interfaz privada.

## Exposición pública

La captura de `nmap` aportada por el responsable en la conversación del 2026-09-25 muestra:

```text
Port 5433 is closed.
Port 6380 is closed.
```

La captura externa suple la comprobación local, que no tenía `nmap` instalado. La evidencia queda registrada como cierre de exposición pública R6.
